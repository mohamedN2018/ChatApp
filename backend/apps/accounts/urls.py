"""Account/auth routes.

Empty in Phase 0 — the user model and infrastructure land first. Registration,
verification, login, token refresh, password reset, session/device management
are added in the authentication phase.
"""

from __future__ import annotations

from django.urls import path

app_name = "accounts"

urlpatterns: list[path] = []
